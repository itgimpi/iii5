#include <bits/stdc++.h>
using namespace std;
// 

int main() { // 

    stack <int> s;
    string rec; cin >> rec; 

    while ( rec != "kraj" ) {
        if (rec == "da") { // da li je prazan
            string r1, r2, r3; cin >> r1 >> r2 >> r3;
            if

        } 
        else if (rec == "gurni") {

        }
        else if (rec == "skloni") {
            
        }
        else if (rec == "gurni") {
            
        }
        else if (rec == "vrh") {
            
        }

        cin >> rec;

    } 
    
}